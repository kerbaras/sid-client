import React from 'react';
import MainPage from '../MainPage';

const NotFound = () => (
    <MainPage>
        <span style={{
            fontWeight: 100,
            fontSize: '64px',
            margin: '64px 0 0',
            textAlign: 'center',
            flex: '1 0 auto'
        }} > Not Found! </span>
    </MainPage>
);

export default NotFound;
